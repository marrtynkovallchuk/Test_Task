SELECT
    COUNT(*) AS total_emails,

    COUNT(delivery_ts) AS delivered,

    COUNT(read_ts) AS opened,

    COUNT(click_ts) AS clicked,

    COUNTIF(total_credits > 0) AS spent_credits
FROM `testtask-497419.Test_Task.Test_Task`;
